#!/bin/bash
#/etc/passwd
#base command: pwd ls find more gzip who mkdir rmdir touch rm cat grep cp mv 
#chmod sh ./ 
#-e error exit; -n read no execture; -u chech var; -v read input line; -x execture and show

AA="HELLO WORD"
echo $AA
test -n "$AA"
test -z "$AA"

# -eq -ge -ne -lt -le
#! -a -o

if [ ] ;then
elif [ ]
fi
case i in 
1)
;;
2)
;;
*)
esac

for i in [ ]
do
done

while
do
done

until
do
done







